#pragma once

template<typename T>
class SkipList{
    private:
        
    public:

        //SkipList();
    private:
        static const unsigned int DEFAULT_SKIPS_COUNT = 8;

        unsigned int maxSkips = DEFAULT_SKIPS_COUNT;


};